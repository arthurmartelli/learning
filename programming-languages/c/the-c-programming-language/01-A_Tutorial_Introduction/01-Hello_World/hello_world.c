#include <stdio.h> // include information abut standard library

int main() // define a function named main that receives no arguments
{          // statements of main are enclosed in braces
    printf("hello, world\n");
    // main calls library function printf
    // to print this sequence of characters;
    // \n represents the newline of characters
}
